import { cleanup } from '@testing-library/react';
import { afterEach, beforeEach, describe, test } from 'vitest';

describe('WatchedMoviesListItem component test suite', () => {
	beforeEach(() => {});

	afterEach(() => {
		cleanup();
	});

	test.todo('should render the component correctly', () => {});

	test.todo('should render the correct movie detials', () => {});

	test.todo('should delete WatchedMoviesListItem if the user clicked the delete button');
});
